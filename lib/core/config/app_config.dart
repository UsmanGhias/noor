/// Application-wide configuration constants.
library;

/// Display name shown in UI and system settings.
const String kAppName = 'Noor ul Haya';

/// Semantic version string (keep in sync with pubspec version).
const String kAppVersion = '1.0.0';

/// Minimum supported Android SDK (API 24).
const int kMinAndroidSdk = 24;
